# 根账号（仅用于登录与分级管理，不会出现在 /accounts 列表中）
# 也可写在 monorepo 根目录的 `.env`，Vite 会先加载根目录再加载本文件
ROOT_USERNAME=root
ROOT_PASSWORD=change-me

# JWT 签名密钥（会话令牌），请使用足够长的随机串
JWT_SECRET=change-me-to-a-long-random-secret

# Supabase
# 本地：`pnpm db:start` 后在终端看「Project URL」与「Authentication Keys」
SUPABASE_URL=http://127.0.0.1:54321
# 本地填终端中的 Secret（等同 service_role）；云端填 Dashboard 的 service_role
SUPABASE_SERVICE_ROLE_KEY=
# 本地：终端「Publishable」；云端：Dashboard 的 anon / public key（用于登录与校验 access_token）
SUPABASE_ANON_KEY=

# Cloudflare Worker：Vite 在 dev/build 时会把上述变量从根目录 `.env` 与本文件读入并写进 Worker 包
# （代码里须使用 `process.env.XXX` 固定属性名，勿用 `process.env[name]`）。
# 线上部署时请在构建机提供相同变量，或另行用 Wrangler / 面板配置。
